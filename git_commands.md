git init

git add .

git commit -m "Example java program"

GitHub account in browser

create repo  name JFS\_VI-SEM

copy http link

git remote add origin (http link)

git push origin main/master

git branch

check your remote repo

**To reflect changes in remote repo if file get changes**

git status 

git add .

git status on branch master

git commit -m "modified java program"



steps

git init

git add .

git commit -m "message"

git remote add origin <remote-url>

git push origin main

check in remote repo

**if you can add any files in  local repo**

git status

git add.

git commit -m "message"

git push origin main

**if you  want to retrieve all the data to your pc(cloning)**

git clone (link)

